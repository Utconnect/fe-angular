import { InputDateRangeDateTextPipe } from './date-text.pipe';

describe('InputDateRangeDateTextPipe', () => {
  it('create an instance', () => {
    const pipe = new InputDateRangeDateTextPipe();
    expect(pipe).toBeTruthy();
  });
});
