import { RequiredStepDirective } from './required-step.directive';

describe('RequiredStepDirective', () => {
  it('should create an instance', () => {
    const directive = new RequiredStepDirective();
    expect(directive).toBeTruthy();
  });
});
