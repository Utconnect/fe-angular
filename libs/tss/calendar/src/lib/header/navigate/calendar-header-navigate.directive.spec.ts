import { TssCalendarHeaderNavigateDirective } from './calendar-header-navigate.directive';

describe('TssCalendarHeaderNavigateDirective', () => {
  it('should create an instance', () => {
    const directive = new TssCalendarHeaderNavigateDirective();
    expect(directive).toBeTruthy();
  });
});
