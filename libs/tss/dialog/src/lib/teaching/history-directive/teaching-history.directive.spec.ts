import { TssTeachingDialogHistoryDirective } from './teaching-history.directive';

describe('TeachingHistoryDirective', () => {
  it('should create an instance', () => {
    const directive = new TssTeachingDialogHistoryDirective();
    expect(directive).toBeTruthy();
  });
});
